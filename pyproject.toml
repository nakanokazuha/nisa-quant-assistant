[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nisa-quant-assistant"
version = "0.1.0"
description = "Local-first, advisory-only NISA research assistant"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
